//! Intentionally empty
//! This file exists, so that doc tests can be used inside binary crates.

mod app;
mod event;
#[allow(dead_code)]
mod reactor;
mod runtime_extensions;
mod runtime_state;
mod wasm;
