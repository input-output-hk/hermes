[package]
name = "staked_ada_indexer_mock"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
shared = { path = "../../../../../apps/athena/shared", version = "0.1.0", features = ["cat-gateway-types"] }

anyhow = "1.0.100"
cardano-blockchain-types = { path = "../../../../../../../catalyst-libs/rust/cardano-blockchain-types" }


[lints]
workspace = true