[package]
edition.workspace = true
name = "staked-ada-indexer"
version = "0.1.0"
license = "MIT OR Apache-2.0"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
shared = { version = "0.1.0", path = "../../shared" }
anyhow = "1.0.100"
